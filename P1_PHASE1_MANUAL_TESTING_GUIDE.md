# P1 Phase 1 - 手動測試指南
**日期**: 2026-01-09
**狀態**: 準備就緒
**預估時間**: 30-45 分鐘

---

## 測試總覽

本指南涵蓋 P1 Phase 1 已完成的三個功能：
1. ✅ Task 1: Review Queue Enhanced Hotkeys
2. ✅ Task 2: Monitoring Time Picker v2
3. ✅ Bonus: Dashboard Quick Links

---

## 前置準備

### 1. 啟動開發伺服器

```bash
cd foodsense-frontend
npm run dev
```

等待伺服器啟動：`http://localhost:3000`

### 2. 啟用 Feature Flags

開啟瀏覽器 Console (F12 → Console)，執行：

```javascript
// 啟用所有 P1 Phase 1 功能
window.__featureFlags.enable('review_queue_enhanced_hotkeys')
window.__featureFlags.enable('monitoring_time_picker_v2')

// 確認狀態
window.__featureFlags.get()
```

預期輸出：
```json
{
  "review_queue_enhanced_hotkeys": true,
  "monitoring_time_picker_v2": true,
  "empty_states_v2": false
}
```

### 3. 準備錄製工具（選擇性）

建議使用：
- **macOS**: QuickTime Player / Screenshot Tool (Cmd+Shift+5)
- **Windows**: Xbox Game Bar (Win+G)
- **Chrome**: Built-in screen recorder extensions

---

## Test 1: Dashboard Quick Links (5 分鐘)

### 測試位置
`http://localhost:3000`

### 測試清單

#### 1.1 視覺檢查
- [ ] 看到「快速連結」區塊
- [ ] 6 個連結卡片排列整齊
- [ ] 每個卡片有圖示、標題、描述
- [ ] 「審核佇列」卡片顯示 badge（pending count）

#### 1.2 Hover 效果
- [ ] Hover 時卡片陰影加深
- [ ] Hover 時箭頭圖示向右移動
- [ ] Transition 流暢（無閃爍）

#### 1.3 導航測試
測試每個連結是否正確跳轉：

| 連結 | 預期 URL | 狀態 |
|-----|---------|------|
| 審核佇列 | `/review/queue` | [ ] |
| 產品列表 | `/products` | [ ] |
| 監控儀表板 | `/monitoring/business` | [ ] |
| 資料品質 | `/data-quality` | [ ] |
| LawCore 規則 | `/lawcore` | [ ] |
| 字典管理 | `/dictionary` | [ ] |

#### 1.4 響應式設計
- [ ] 桌面版：3 欄排列
- [ ] 平板版 (調整視窗寬度)：2 欄排列
- [ ] 手機版 (調整視窗寬度)：1 欄排列

### 通過標準
✅ 所有連結正確導航
✅ Hover 效果流暢
✅ 響應式佈局正常

---

## Test 2: Review Queue Enhanced Hotkeys (15 分鐘)

### 測試位置
`http://localhost:3000/review/queue`

### 前置條件
- 確保審核佇列有資料（至少 3-5 筆）
- Feature flag 已啟用

### 測試清單

#### 2.1 Help Modal
- [ ] 按 `?` (Shift + /) 打開說明視窗
- [ ] 說明視窗顯示所有快捷鍵
- [ ] 看到「Enhanced Mode」標記
- [ ] 快捷鍵分類清楚（Navigation, Selection, Review Actions, Global）
- [ ] 按 `ESC` 或點擊外部關閉視窗

#### 2.2 Vim-style Navigation (NEW)
- [ ] 按 `j` 移動到下一筆記錄（向下）
- [ ] 按 `k` 移動到上一筆記錄（向上）
- [ ] 連續按 `j` `j` `j` 快速向下移動
- [ ] 連續按 `k` `k` 快速向上移動
- [ ] 在第一筆按 `k` 無反應（邊界檢查）
- [ ] 在最後一筆按 `j` 無反應（邊界檢查）

#### 2.3 Legacy Navigation (Backward Compatible)
- [ ] 按 `n` 移動到下一筆（應該還能用）
- [ ] 按 `p` 移動到上一筆（應該還能用）

#### 2.4 Quick Actions (NEW)
測試每個 action 並觀察 toast 通知：

| 快捷鍵 | 動作 | Toast 顏色 | Toast 文字 | 狀態 |
|-------|-----|-----------|-----------|------|
| `Shift+A` | Quick Approve | 綠色 | ✅ 已核准 | [ ] |
| `Shift+R` | Quick Reject | 紅色 | ❌ 已拒絕 | [ ] |
| `i` | Inspect Product | 無 | (打開 modal) | [ ] |
| `f` | Flag for Review | 黃色 | 🚩 已標記 | [ ] |

#### 2.5 Inspect Modal (i 鍵)
- [ ] 按 `i` 打開產品詳情 modal
- [ ] Modal 顯示產品資訊
- [ ] Modal 內按快捷鍵**無反應**（避免衝突）
- [ ] 關閉 modal 後快捷鍵恢復運作

#### 2.6 Text Input Detection
- [ ] 點擊 search bar 或任何輸入欄位
- [ ] 在輸入欄位內按 `j` / `k`，應該輸入字母而非觸發快捷鍵
- [ ] 離開輸入欄位後快捷鍵恢復運作

#### 2.7 Performance Test
完成一次完整審核流程並計時：

**BEFORE (Legacy)**:
1. 滑鼠點擊選擇記錄
2. 滑鼠點擊 review 按鈕
3. 選擇 approve/reject
4. 點擊確認
5. 移動到下一筆

預期時間：~45 秒

**AFTER (Enhanced)**:
1. 按 `j` 選擇記錄
2. 按 `Shift+A` 核准（或 `Shift+R` 拒絕）
3. Toast 出現
4. 按 `j` 移動到下一筆

預期時間：~10-15 秒

- [ ] 記錄 BEFORE 時間：_____ 秒
- [ ] 記錄 AFTER 時間：_____ 秒
- [ ] 計算改善率：_____%

目標：**至少 70% 更快**

### 通過標準
✅ 所有快捷鍵正常運作
✅ Toast 通知正確顯示
✅ 無快捷鍵衝突
✅ 效能改善 >= 70%

---

## Test 3: Monitoring Time Picker v2 (15 分鐘)

### 測試位置
- `http://localhost:3000/monitoring/app`
- `http://localhost:3000/monitoring/business`
- `http://localhost:3000/monitoring/infra`

### 測試清單

#### 3.1 Preset Options
測試所有 6 個預設選項：

| Preset | 標籤 | 預期行為 | 狀態 |
|--------|-----|---------|------|
| `1h` | 最近 1 小時 | 顯示 1 小時資料 | [ ] |
| `6h` | 最近 6 小時 (NEW) | 顯示 6 小時資料 | [ ] |
| `24h` | 最近 24 小時 | 顯示 24 小時資料 | [ ] |
| `7d` | 最近 7 天 | 顯示 7 天資料 | [ ] |
| `30d` | 最近 30 天 (NEW) | 顯示 30 天資料 | [ ] |
| `custom` | 自定義範圍 (NEW) | 打開日期選擇器 | [ ] |

#### 3.2 Custom Range Picker
- [ ] 點擊「自定義範圍」
- [ ] Modal 打開顯示日期時間選擇器
- [ ] 選擇開始日期時間 (Start)
- [ ] 選擇結束日期時間 (End)
- [ ] 點擊「套用」
- [ ] Modal 關閉，URL 更新
- [ ] 圖表更新顯示自定義範圍資料

#### 3.3 URL Parameter Persistence
測試 URL 分享功能：

1. **設定自定義範圍**:
   - [ ] 選擇 custom 並設定範圍
   - [ ] 確認 URL 包含 `start_time` 和 `end_time` 參數
   - [ ] 範例：`?start_time=2026-01-08T00:00&end_time=2026-01-09T23:59`

2. **複製 URL 測試**:
   - [ ] 複製當前 URL
   - [ ] 開新分頁貼上 URL
   - [ ] 時間範圍保持一致

3. **刷新頁面測試**:
   - [ ] 設定自定義範圍
   - [ ] 按 F5 刷新頁面
   - [ ] 時間範圍保持一致

#### 3.4 Multi-Page Integration
測試 3 個監控頁面都正常運作：

| 頁面 | URL | Time Picker 顯示 | 狀態 |
|------|-----|----------------|------|
| App Monitoring | `/monitoring/app` | ✅ 顯示 | [ ] |
| Business Monitoring | `/monitoring/business` | ✅ 顯示 | [ ] |
| Infra Monitoring | `/monitoring/infra` | ✅ 顯示 | [ ] |

#### 3.5 Backward Compatibility
測試關閉 feature flag 後的行為：

```javascript
// 關閉 feature flag
window.__featureFlags.disable('monitoring_time_picker_v2')
```

- [ ] 重新載入頁面
- [ ] 看到舊版 time picker（只有 3 個選項：1h, 24h, 7d）
- [ ] 舊版 picker 正常運作

```javascript
// 重新啟用
window.__featureFlags.enable('monitoring_time_picker_v2')
```

#### 3.6 Performance Test
測試調查「昨天下午 2-4 點的流量異常」：

**BEFORE (Legacy v1)**:
1. 點擊 time picker (2 clicks)
2. 只能選 24h（無法精確選時間）
3. 手動檢視整天資料找到 2-4pm
4. 預估時間：~120-180 秒

**AFTER (Enhanced v2)**:
1. 點擊 time picker → Custom (2 clicks)
2. 設定開始：昨天 14:00
3. 設定結束：昨天 16:00
4. 點擊套用
5. 預估時間：~15-30 秒

- [ ] 記錄 BEFORE 時間：_____ 秒
- [ ] 記錄 AFTER 時間：_____ 秒
- [ ] 計算改善率：_____%

目標：**至少 80% 更快**

### 通過標準
✅ 所有 6 個 presets 正常運作
✅ Custom range picker 功能完整
✅ URL 參數正確保存和還原
✅ 3 個頁面都正常整合
✅ Backward compatible (flag OFF 時回到 v1)
✅ 效能改善 >= 80%

---

## Test Summary Form

### 測試結果記錄

**測試日期**: _______________
**測試人員**: _______________
**環境**: Local Dev (http://localhost:3000)

#### Test 1: Dashboard Quick Links
- 狀態: [ ] PASS [ ] FAIL [ ] PARTIAL
- 問題: _______________________

#### Test 2: Review Queue Enhanced Hotkeys
- 狀態: [ ] PASS [ ] FAIL [ ] PARTIAL
- 效能改善: _____%
- 問題: _______________________

#### Test 3: Monitoring Time Picker v2
- 狀態: [ ] PASS [ ] FAIL [ ] PARTIAL
- 效能改善: _____%
- 問題: _______________________

### 整體評估

**總體狀態**: [ ] PASS [ ] NEEDS WORK

**關鍵發現**:
- ___________________________
- ___________________________
- ___________________________

**建議**:
- ___________________________
- ___________________________

**準備部署**: [ ] YES [ ] NO [ ] NEEDS FIXES

---

## Troubleshooting

### Feature Flags 不生效
```javascript
// 清除 localStorage
localStorage.clear()

// 重新啟用
window.__featureFlags.enable('review_queue_enhanced_hotkeys')
window.__featureFlags.enable('monitoring_time_picker_v2')

// 刷新頁面
location.reload()
```

### 快捷鍵不運作
1. 檢查是否在輸入欄位內（應該無反應）
2. 檢查 feature flag 是否啟用
3. 檢查 browser console 是否有錯誤
4. 重新載入頁面

### Time Picker 沒更新到 v2
1. 確認 feature flag 啟用
2. Hard refresh: Cmd+Shift+R (Mac) / Ctrl+Shift+R (Windows)
3. 清除 cache 後重新載入

---

## 下一步

### 測試完成後：
1. [ ] 填寫測試結果表單
2. [ ] 截圖或錄影記錄關鍵功能
3. [ ] 記錄任何發現的 bug
4. [ ] 評估是否準備好進入生產環境

### 如果發現 Bug：
1. 記錄重現步驟
2. 截圖或錄影
3. 記錄錯誤訊息（browser console）
4. 報告給開發團隊

---

**文檔版本**: v1.0
**最後更新**: 2026-01-09
**預估測試時間**: 30-45 分鐘
**下個任務**: P1 Task 3 - Empty States v2
